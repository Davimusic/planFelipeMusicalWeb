"use client"; //no sé por qué lo necesita

import RootLayout from "./layout";

function Home() {
  const { tasks } = useTasks();
    return (
      <div >  
      </div>
    );
}

export default Home;
